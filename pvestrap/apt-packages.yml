---
- name: APT packages
  hosts: all

  vars:
    suite: trixie

  tasks:
    - name: Remove sources.list
      ansible.builtin.file:
        state: absent
        path: "{{ item }}"
      loop:
        - /etc/apt/sources.list
        - /etc/apt/sources.list.d/pbs-enterprise.list
        - /etc/apt/sources.list.d/pbs-enterprise.sources
        - /etc/apt/sources.list.d/pve.list
        - /etc/apt/sources.list.d/pve.list.bak
        - /etc/apt/sources.list.d/pve-enterprise.list
        - /etc/apt/sources.list.d/pve-enterprise.sources
    - name: Ensure DEB822 sources file (Debian)
      ansible.builtin.copy:
        dest: /etc/apt/sources.list.d/debian.sources
        owner: root
        group: root
        mode: '0644'
        content: |
          Types: deb
          URIs: https://mirrors.ustc.edu.cn/debian/
          Suites: {{ suite }}
          Components: main contrib non-free non-free-firmware
          Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg

          Types: deb
          URIs: https://mirrors.ustc.edu.cn/debian/
          Suites: {{ suite }}-updates
          Components: main contrib non-free non-free-firmware
          Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg

          Types: deb
          URIs: https://mirrors.ustc.edu.cn/debian-security/
          Suites: {{ suite }}-security
          Components: main contrib non-free non-free-firmware
          Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg
    - name: Ensure DEB822 sources file (PVE)
      ansible.builtin.copy:
        dest: /etc/apt/sources.list.d/pve.sources
        owner: root
        group: root
        mode: '0644'
        content: |
          Types: deb
          URIs: https://mirrors.ustc.edu.cn/proxmox/debian/pve/
          Suites: {{ suite }}
          Components: pve-no-subscription
          Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg

          Types: deb
          URIs: https://mirrors.ustc.edu.cn/proxmox/debian/pbs/
          Suites: {{ suite }}
          Components: pbs-no-subscription
          Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
    - name: Ensure APT settings
      ansible.builtin.copy:
        dest: /etc/apt/apt.conf.d/51unattended-upgrades-custom
        content: |
          Unattended-Upgrade::Origins-Pattern {
              "origin=Debian,codename=\${distro_codename}-updates,label=Debian";
              "origin=Proxmox,codename=\${distro_codename}";
              "origin=InfluxDB";
          };
        mode: "0644"
    - name: Configure influxdata repo
      ansible.builtin.import_tasks: tasks/influxdata-archive-keyring.yml
    - name: Upgrade all packages
      ansible.builtin.apt:
        upgrade: dist
        update_cache: true
    - name: Install PBS
      when: inventory_hostname == "pv1"
      ansible.builtin.apt:
        state: present
        name:
          - apt-listbugs
          - proxmox-backup
    - name: Remove unused packages
      when: inventory_hostname != "pv1"
      ansible.builtin.apt:
        state: absent
        name:
          - apt-listbugs
          - proxmox-backup
          - systemd-timesyncd
    - name: Install grub-efi
      ansible.builtin.apt:
        state: present
        name:
          - grub-efi
    - name: Install Vlab custom PVE packages
      ansible.builtin.import_tasks: tasks/pve-custom-packages.yml
    - name: Install packages
      ansible.builtin.apt:
        state: present
        name:
          - apt-listchanges
          - bmon
          - bpfcc-tools
          - bpftrace
          - btrfs-progs
          - bwm-ng
          - chrony
          - duperemove
          - fd-find
          - htop
          - intel-microcode
          - iotop-c
          - ipmitool
          - iptables-persistent
          - iputils-ping
          - jq
          - linux-sysctl-defaults
          - lldpd
          - ncdu
          - net-tools
          - netcat-openbsd
          - proxmox-ve
          - pv
          - rasdaemon
          - ripgrep
          - rsyslog
          - ssh
          - strace
          - sysstat
          - tcpdump
          - testdisk
          - unattended-upgrades
          - vim
          - virtiofsd
          - wget
    - name: Auto-remove unused packages
      ansible.builtin.apt:
        autoremove: true
        purge: true
